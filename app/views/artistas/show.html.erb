<p id="notice"><%= notice %></p>

<p>
  <b><%= t 'nombre' %>:</b>
  <%= @artista.nombre %>
</p>

<p>
  <b><%= t 'url' %>:</b>
  <%= @artista.url %>
</p>

	<div class="form-actions">
		<%= link_to t('editar').capitalize, edit_artista_path(@artista), :class => 'btn' %>
		<%= link_to t('volver').capitalize, artistas_path, :class => 'btn' %>
	</div>
